{
  "name": "avm-ui-workspaces",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.1.1",
  "publishConfig": {
    "registry": "http://localhost:4873/"
  },
  "scripts": {
    "构建压缩版本": "avm-ui B",
    "构建未压缩版本": "avm-ui B -d0",
    "为可视化工具生成 JSON": "avm-ui J"
  }
}
